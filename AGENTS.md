# Agent Instructions

Start with `.codex-workflow.md`.

This repository uses a Karpathy-style AI workflow:

- `.codex-workflow.md`: operating manual.
- `.codex-agents/`: lightweight agent roles.
- `.codex-memory/`: durable markdown memory.
- `.codex-memory/guard.md`: memory safety guard.

The `.codex` file exists but is read-only in this workspace, so the workflow is
stored in the files above.
